# 📋 Как применить SQL миграции

## ✅ Способ 1: Через Supabase Dashboard (САМЫЙ ПРОСТОЙ)

### Шаг 1: Откройте Dashboard
1. Перейдите на https://supabase.com/dashboard/project/zaheofzxbfqabdxdmjtz
2. Войдите в свой аккаунт

### Шаг 2: Откройте SQL Editor
1. В левом меню найдите **"SQL Editor"** (иконка с символом `</>`)
2. Нажмите на него

### Шаг 3: Скопируйте код миграции
1. Откройте файл `supabase/migrations/20251219_security_tables.sql` в вашем проекте
2. Выделите **ВЕСЬ** код (Ctrl+A)
3. Скопируйте (Ctrl+C)

### Шаг 4: Вставьте и выполните
1. В SQL Editor вставьте скопированный код (Ctrl+V)
2. Нажмите кнопку **"Run"** (или нажмите Ctrl+Enter)
3. Дождитесь выполнения (обычно несколько секунд)

### Шаг 5: Проверьте результат
1. В левом меню перейдите в **"Database"** → **"Tables"**
2. Должны появиться новые таблицы:
   - ✅ `subscriptions`
   - ✅ `user_connections`
   - ✅ `api_usage_logs`

**Готово!** Миграция применена.

---

## 🔧 Способ 2: Через Supabase CLI (для продвинутых)

### Требования:
- Установлен Supabase CLI
- Выполнен `supabase login`
- Проект привязан через `supabase link`

### Команды:

```bash
# 1. Перейдите в корень проекта
cd c:\Users\Full_Errorist\Documents\GitHub\ai-sql-advisor-next

# 2. Привяжитесь к проекту (если еще не привязаны)
supabase link --project-ref zaheofzxbfqabdxdmjtz

# 3. Примените миграции
supabase db push
```

---

## ⚠️ Что делать, если возникла ошибка?

### Ошибка: "relation already exists"
**Причина:** Таблицы уже созданы ранее  
**Решение:** Это нормально, миграция использует `CREATE TABLE IF NOT EXISTS`, поэтому можно игнорировать

### Ошибка: "permission denied"
**Причина:** Недостаточно прав  
**Решение:** Убедитесь, что вы вошли в правильный проект и у вас есть права администратора

### Ошибка: "function already exists"
**Причина:** Функция `execute_sql` уже создана  
**Решение:** Это нормально, миграция использует `CREATE OR REPLACE FUNCTION`, поэтому функция обновится

---

## 📝 Что создается миграцией?

1. **Таблица `subscriptions`** - управление подписками пользователей
2. **Таблица `user_connections`** - хранение подключений к БД (зашифрованных)
3. **Таблица `api_usage_logs`** - логирование использования API
4. **RPC функция `execute_sql`** - безопасное выполнение SQL запросов
5. **RLS политики** - защита данных на уровне строк
6. **Индексы** - для быстрого поиска
7. **Триггеры** - для автоматического обновления `updated_at`

---

## ✅ После применения миграции

1. **Создайте тестовую подписку** (для проверки работы):

```sql
-- В SQL Editor выполните:
INSERT INTO subscriptions (user_id, plan, status, current_period_end)
VALUES (
  'ВАШ_USER_UUID',  -- Замените на реальный UUID из auth.users
  'pro',
  'active',
  NOW() + INTERVAL '30 days'
);
```

2. **Проверьте UUID пользователя:**

```sql
-- В SQL Editor выполните:
SELECT id, email FROM auth.users;
-- Скопируйте UUID нужного пользователя
```

---

## 🎯 Итог

После применения миграции:
- ✅ Таблицы созданы
- ✅ RLS политики настроены
- ✅ RPC функция готова
- ✅ Можно деплоить Edge Functions

**Следующий шаг:** Задеплойте исправленные Edge Functions (см. `DEPLOY_INSTRUCTIONS.md`)
