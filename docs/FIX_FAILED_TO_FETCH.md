# 🔧 Исправление ошибки "Failed to fetch"

## ❌ Проблема

Ошибка: `TypeError: Failed to fetch`

**Причина:** Запрос к Supabase Edge Function не может быть выполнен. Это может быть из-за:
1. Edge Function не задеплоена
2. Неправильный URL
3. Проблемы с CORS
4. Проблемы с сетью/интернетом

---

## 🔍 Диагностика

### Шаг 1: Проверьте переменные окружения

1. Убедитесь, что файл `.env.local` существует в корне проекта
2. Проверьте, что в нем есть:

```env
NEXT_PUBLIC_SUPABASE_URL=https://zaheofzxbfqabdxdmjtz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
```

3. **ВАЖНО:** После изменения `.env.local` перезапустите dev-сервер:
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

---

### Шаг 2: Проверьте, задеплоена ли Edge Function

1. Откройте: https://supabase.com/dashboard/project/zaheofzxbfqabdxdmjtz
2. Перейдите в **Edge Functions**
3. Проверьте, что функция `generate_sql` существует и задеплоена
4. Если функции нет или она не задеплоена:
   - См. инструкцию в `DEPLOY_INSTRUCTIONS.md`
   - Или задеплойте через Dashboard:
     - Edge Functions → `generate_sql` → Edit
     - Вставьте код из `supabase/functions/generate_sql/index.ts`
     - Нажмите **Deploy**

---

### Шаг 3: Проверьте URL запроса

В консоли браузера (F12) выполните:

```javascript
// Проверить переменные окружения
console.log('SUPABASE_URL:', process.env.NEXT_PUBLIC_SUPABASE_URL);
console.log('ANON_KEY:', process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ? 'Есть' : 'НЕТ');

// Проверить URL запроса
const BASE = `${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1`;
console.log('BASE URL:', BASE);
console.log('Full URL:', `${BASE}/generate_sql`);
```

**Ожидаемый результат:**
```
BASE URL: https://zaheofzxbfqabdxdmjtz.supabase.co/functions/v1
Full URL: https://zaheofzxbfqabdxdmjtz.supabase.co/functions/v1/generate_sql
```

Если URL неправильный → проверьте `.env.local`

---

### Шаг 4: Проверьте JWT токен

В консоли браузера:

```javascript
const token = localStorage.getItem('jwt');
console.log('JWT токен:', token ? token.substring(0, 50) + '...' : 'НЕТ');

if (!token) {
  console.error('❌ JWT токен отсутствует! Нужно войти через /auth');
}
```

---

### Шаг 5: Проверьте запрос вручную

В консоли браузера выполните:

```javascript
const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL || 'https://zaheofzxbfqabdxdmjtz.supabase.co';
const ANON_KEY = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
const token = localStorage.getItem('jwt');

console.log('Тестирую запрос к Edge Function...');

fetch(`${SUPABASE_URL}/functions/v1/generate_sql`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token || ANON_KEY}`,
    'apikey': ANON_KEY
  },
  body: JSON.stringify({
    nl: 'test query',
    schema: {},
    dialect: 'postgres'
  })
})
  .then(r => {
    console.log('Статус ответа:', r.status);
    return r.text();
  })
  .then(text => {
    console.log('Ответ:', text);
  })
  .catch(e => {
    console.error('❌ Ошибка:', e);
    console.error('Детали:', e.message);
  });
```

**Результаты:**
- Если статус `200` → Edge Function работает, проблема в коде приложения
- Если статус `401` → Проблема с авторизацией (см. `FIX_SUBSCRIPTION_ERROR.md`)
- Если статус `403` → Проблема с подпиской (см. `FIX_SUBSCRIPTION_ERROR.md`)
- Если `Failed to fetch` → Проблема с сетью или CORS

---

## 🔧 Решения

### Решение 1: Edge Function не задеплоена

**Симптомы:** Запрос возвращает 404 или "Failed to fetch"

**Решение:**
1. Задеплойте Edge Function (см. `DEPLOY_INSTRUCTIONS.md`)
2. Или через Dashboard:
   - Edge Functions → `generate_sql` → Edit
   - Вставьте код из `supabase/functions/generate_sql/index.ts`
   - Deploy

---

### Решение 2: Неправильный URL

**Симптомы:** `Failed to fetch` или 404

**Решение:**
1. Проверьте `.env.local`:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://zaheofzxbfqabdxdmjtz.supabase.co
   ```
   **ВАЖНО:** Без `/functions/v1` в конце!

2. Перезапустите dev-сервер после изменения `.env.local`

---

### Решение 3: Проблема с CORS

**Симптомы:** `Failed to fetch` в консоли, но запрос виден в Network tab

**Решение:**
1. Проверьте, что в Edge Function `generate_sql` есть правильные CORS заголовки
2. Убедитесь, что ваш домен в списке `ALLOWED_ORIGINS`:
   ```typescript
   const ALLOWED_ORIGINS = [
     'https://ai-sql-advisor.vercel.app',
     'http://localhost:3000',
     'http://localhost:3001'
   ];
   ```
3. Если вы используете другой порт, добавьте его в список

---

### Решение 4: Проблема с сетью/интернетом

**Симптомы:** `Failed to fetch` на всех запросах

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте, не блокирует ли файрвол/антивирус запросы
3. Попробуйте другой браузер
4. Попробуйте отключить VPN (если используется)

---

### Решение 5: Проблема с JWT токеном

**Симптомы:** `Failed to fetch` или 401

**Решение:**
1. Перелогиньтесь через `/auth`
2. Проверьте, что токен сохранился:
   ```javascript
   console.log('JWT:', localStorage.getItem('jwt'));
   ```

---

## 🎯 Быстрое решение (чек-лист)

- [ ] Проверен `.env.local` (правильный URL и ANON_KEY)
- [ ] Dev-сервер перезапущен после изменения `.env.local`
- [ ] Edge Function `generate_sql` задеплоена
- [ ] JWT токен присутствует в `localStorage`
- [ ] Интернет-соединение работает
- [ ] Попробовано в другом браузере

---

## 📊 Проверка через Network tab

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте сгенерировать SQL запрос
4. Найдите запрос к `/functions/v1/generate_sql`
5. Проверьте:
   - **Status:** Должен быть 200 (не 404, не Failed)
   - **Request URL:** Должен быть правильным
   - **Request Headers:** Должен быть `Authorization: Bearer ...`
   - **Response:** Должен быть JSON (не ошибка)

---

## 🔍 Детальная диагностика

### Проверка 1: Правильность URL

```javascript
// В консоли браузера
const expected = 'https://zaheofzxbfqabdxdmjtz.supabase.co/functions/v1/generate_sql';
const actual = `${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1/generate_sql`;
console.log('Ожидается:', expected);
console.log('Фактически:', actual);
console.log('Совпадает:', expected === actual);
```

### Проверка 2: Доступность Edge Function

```javascript
// В консоли браузера
fetch('https://zaheofzxbfqabdxdmjtz.supabase.co/functions/v1/generate_sql', {
  method: 'OPTIONS'
})
  .then(r => console.log('OPTIONS статус:', r.status))
  .catch(e => console.error('Ошибка:', e));
```

Если `OPTIONS` возвращает 200 → Edge Function доступна  
Если `Failed to fetch` → Edge Function не задеплоена или недоступна

---

## ⚠️ Частые ошибки

### Ошибка 1: "NEXT_PUBLIC_SUPABASE_URL is undefined"

**Причина:** Переменная окружения не загружена  
**Решение:**
1. Проверьте `.env.local`
2. Перезапустите dev-сервер
3. Убедитесь, что переменная начинается с `NEXT_PUBLIC_`

### Ошибка 2: "CORS policy: No 'Access-Control-Allow-Origin'"

**Причина:** Проблема с CORS в Edge Function  
**Решение:**
1. Проверьте, что Edge Function задеплоена с правильными CORS заголовками
2. Убедитесь, что ваш домен в `ALLOWED_ORIGINS`

### Ошибка 3: "404 Not Found" на `/functions/v1/generate_sql`

**Причина:** Edge Function не задеплоена  
**Решение:** Задеплойте Edge Function (см. `DEPLOY_INSTRUCTIONS.md`)

---

## 📞 Если ничего не помогает

1. **Проверьте логи Edge Function:**
   - Supabase Dashboard → Edge Functions → `generate_sql` → Logs
   - Ищите ошибки в логах

2. **Проверьте логи dev-сервера:**
   - В терминале, где запущен `npm run dev`
   - Ищите ошибки при запуске

3. **Проверьте Network tab:**
   - DevTools → Network
   - Найдите запрос к `generate_sql`
   - Посмотрите детали запроса и ответа

---

## 🎯 Итог

**Самое важное:**
1. ✅ Edge Function должна быть задеплоена
2. ✅ `.env.local` должен содержать правильные переменные
3. ✅ Dev-сервер должен быть перезапущен после изменения `.env.local`
4. ✅ JWT токен должен быть в `localStorage`

После проверки всех пунктов ошибка должна исчезнуть! 🎉
