# 🔒 План тестирования безопасности и RLS

## 📋 Обзор

Комплексное тестирование безопасности для всех тарифов (Free, Light, Pro) с учетом:
- Row Level Security (RLS)
- SQL Injection защита
- XSS защита
- Защита от накрутки токенов
- Конфиденциальность данных
- Защита баз данных пользователей

---

## 🎯 Тестовые сценарии по тарифам

### 🆓 Free план (3 дня)

#### 1. Лимиты токенов
- ✅ **Тест:** Использовать 100,000 токенов
- ✅ **Ожидание:** Генерация SQL работает
- ✅ **Тест:** Попытка использовать 100,001 токен
- ✅ **Ожидание:** Блокировка генерации SQL, ошибка "Token limit exceeded"

#### 2. Лимиты открытий таблиц
- ✅ **Тест:** Открыть таблицу 20 раз
- ✅ **Ожидание:** Все открытия работают
- ✅ **Тест:** Попытка открыть 21-й раз
- ✅ **Ожидание:** Блокировка, ошибка "Table opens limit exceeded"

#### 3. Лимиты скачиваний
- ✅ **Тест:** Скачать файл 20 раз
- ✅ **Ожидание:** Все скачивания работают
- ✅ **Тест:** Попытка скачать 21-й раз
- ✅ **Ожидание:** Блокировка, ошибка "Download limit exceeded"

#### 4. Сгорание токенов через 3 дня
- ✅ **Тест:** Создать Free аккаунт, подождать 3 дня
- ✅ **Ожидание:** Токены сброшены, счетчики обнулены

---

### 💡 Light план ($3/мес)

#### 1. Лимиты токенов
- ✅ **Тест:** Использовать 1,000,000 токенов
- ✅ **Ожидание:** Генерация SQL работает
- ✅ **Тест:** Попытка использовать 1,000,001 токен
- ✅ **Ожидание:** Блокировка или предложение покупки токенов

#### 2. Лимиты открытий таблиц
- ✅ **Тест:** Открыть таблицу 50 раз
- ✅ **Ожидание:** Все открытия работают
- ✅ **Тест:** Попытка открыть 51-й раз
- ✅ **Ожидание:** Блокировка или предложение апгрейда

#### 3. Покупка дополнительных токенов
- ✅ **Тест:** Купить 1M токенов за $1
- ✅ **Ожидание:** Токены добавлены к балансу

---

### ⚡ Pro план ($7/мес)

#### 1. Безлимитные токены
- ✅ **Тест:** Использовать 2,500,000+ токенов
- ✅ **Ожидание:** Генерация SQL работает (до лимита 2.5M)

#### 2. Безлимитные открытия
- ✅ **Тест:** Открыть таблицу 100+ раз
- ✅ **Ожидание:** Все открытия работают без ограничений

---

## 🛡️ Тестирование безопасности

### 1. SQL Injection защита

#### Тест 1: Базовый SQL Injection
```sql
-- Попытка инъекции в запрос пользователя
SELECT * FROM users; DROP TABLE users; --
```
- ✅ **Ожидание:** Запрос отклонен, ошибка валидации

#### Тест 2: Комментарии с инъекцией
```sql
SELECT * FROM users -- DROP TABLE users
```
- ✅ **Ожидание:** Запрос отклонен

#### Тест 3: UNION SELECT инъекция
```sql
SELECT * FROM users UNION SELECT * FROM passwords
```
- ✅ **Ожидание:** Запрос отклонен (если не явно разрешен UNION ALL)

#### Тест 4: Опасные операции
```sql
DELETE FROM users WHERE id = 1
UPDATE users SET password = 'hacked'
DROP TABLE users
```
- ✅ **Ожидание:** Все запросы отклонены

#### Тест 5: Параметризованные запросы
- ✅ **Тест:** Использовать параметризованные запросы в API
- ✅ **Ожидание:** Все параметры экранированы

---

### 2. XSS защита

#### Тест 1: XSS в SQL результатах
```sql
SELECT '<script>alert("XSS")</script>' as name
```
- ✅ **Ожидание:** HTML экранирован в UI

#### Тест 2: XSS в пользовательском вводе
- ✅ **Тест:** Ввести `<script>alert("XSS")</script>` в поле запроса
- ✅ **Ожидание:** Текст экранирован, скрипт не выполняется

---

### 3. Защита от накрутки токенов

#### Тест 1: Прямое обновление токенов
```sql
-- Попытка напрямую обновить токены
UPDATE user_token_usage SET tokens_used = 0 WHERE user_id = '...'
```
- ✅ **Ожидание:** RLS блокирует обновление

#### Тест 2: Отрицательные токены
```sql
-- Попытка добавить отрицательные токены
SELECT add_user_tokens('user_id', -100000)
```
- ✅ **Ожидание:** Ошибка "Cannot add negative tokens"

#### Тест 3: Слишком много токенов за раз
```sql
-- Попытка добавить слишком много токенов
SELECT add_user_tokens('user_id', 200000)
```
- ✅ **Ожидание:** Ошибка "Too many tokens to add at once"

#### Тест 4: Превышение лимита плана
```sql
-- Попытка превысить лимит плана
-- Free: 100K, пытаемся добавить еще 50K когда уже использовано 60K
```
- ✅ **Ожидание:** Ошибка "Token limit exceeded for plan free"

#### Тест 5: Rate Limiting
- ✅ **Тест:** Отправить 11 запросов в минуту
- ✅ **Ожидание:** 11-й запрос блокируется

---

### 4. Конфиденциальность данных

#### Тест 1: Доступ к чужим подпискам
```sql
-- Попытка прочитать чужую подписку
SELECT * FROM subscriptions WHERE user_id != auth.uid()
```
- ✅ **Ожидание:** RLS блокирует, возвращает только свои данные

#### Тест 2: Доступ к чужим токенам
```sql
-- Попытка прочитать чужие токены
SELECT * FROM user_token_usage WHERE user_id != auth.uid()
```
- ✅ **Ожидание:** RLS блокирует

#### Тест 3: Доступ к чужим подключениям
```sql
-- Попытка прочитать чужие connection strings
SELECT * FROM user_connections WHERE user_id != auth.uid()
```
- ✅ **Ожидание:** RLS блокирует

#### Тест 4: Пароли в connection strings
- ✅ **Тест:** Попытка сохранить connection string с паролем в открытом виде
- ✅ **Ожидание:** Ошибка валидации, пароль должен быть зашифрован

---

### 5. Защита баз данных пользователей

#### Тест 1: Read-only подключения
- ✅ **Тест:** Попытка выполнить UPDATE/DELETE через наше приложение
- ✅ **Ожидание:** Запрос отклонен на уровне валидации

#### Тест 2: Изоляция подключений
- ✅ **Тест:** Пользователь A не может получить доступ к БД пользователя B
- ✅ **Ожидание:** Каждый пользователь работает только со своими подключениями

#### Тест 3: Валидация connection strings
- ✅ **Тест:** Попытка подключиться к несуществующей БД
- ✅ **Ожидание:** Ошибка подключения, но без раскрытия внутренней информации

---

## 🔍 Тестирование RLS политик

### Таблица: subscriptions
- ✅ Пользователь видит только свою подписку
- ✅ Пользователь не может обновить чужую подписку
- ✅ Пользователь не может создать подписку для другого пользователя

### Таблица: user_token_usage
- ✅ Пользователь видит только свои токены
- ✅ Пользователь не может напрямую обновить токены (только через RPC)
- ✅ Система может обновлять токены через RPC функции

### Таблица: user_table_opens
- ✅ Пользователь видит только свои счетчики
- ✅ Пользователь не может напрямую обновить счетчики (только через RPC)
- ✅ Система может обновлять счетчики через RPC функции

### Таблица: user_connections
- ✅ Пользователь видит только свои подключения
- ✅ Пользователь не может видеть connection strings других пользователей
- ✅ Пользователь не может обновить чужие подключения

### Таблица: api_usage_logs
- ✅ Пользователь видит только свои логи
- ✅ Система может создавать логи для всех пользователей

### Таблица: security_audit_log
- ✅ Пользователи не имеют доступа (только service role)
- ✅ Логи доступны только для администраторов

---

## 📝 Чек-лист тестирования

### Подготовка
- [ ] Создать тестовые аккаунты для каждого тарифа (Free, Light, Pro)
- [ ] Настроить тестовые подключения к БД
- [ ] Подготовить тестовые SQL запросы (безопасные и опасные)

### Тестирование Free плана
- [ ] Проверить лимит токенов (100K)
- [ ] Проверить лимит открытий (20)
- [ ] Проверить лимит скачиваний (20)
- [ ] Проверить сгорание через 3 дня

### Тестирование Light плана
- [ ] Проверить лимит токенов (1M)
- [ ] Проверить лимит открытий (50)
- [ ] Проверить покупку дополнительных токенов

### Тестирование Pro плана
- [ ] Проверить лимит токенов (2.5M)
- [ ] Проверить безлимитные открытия

### Тестирование безопасности
- [ ] SQL Injection тесты (5 сценариев)
- [ ] XSS тесты (2 сценария)
- [ ] Защита от накрутки токенов (5 тестов)
- [ ] Конфиденциальность данных (4 теста)
- [ ] Защита БД пользователей (3 теста)

### Тестирование RLS
- [ ] subscriptions (3 проверки)
- [ ] user_token_usage (3 проверки)
- [ ] user_table_opens (3 проверки)
- [ ] user_connections (3 проверки)
- [ ] api_usage_logs (2 проверки)
- [ ] security_audit_log (2 проверки)

### Rate Limiting
- [ ] 10 запросов/минуту
- [ ] 100 запросов/час
- [ ] 1,000 запросов/день

---

## 🚨 Ожидаемые результаты

### Успешные тесты
- ✅ Все лимиты работают корректно
- ✅ RLS блокирует несанкционированный доступ
- ✅ SQL Injection попытки отклоняются
- ✅ XSS атаки блокируются
- ✅ Накрутка токенов невозможна
- ✅ Конфиденциальные данные защищены

### Критические ошибки (требуют немедленного исправления)
- ❌ Пользователь может обойти лимиты
- ❌ SQL Injection возможен
- ❌ Доступ к чужим данным
- ❌ Накрутка токенов возможна

---

## 📊 Отчет о тестировании

После выполнения всех тестов создать отчет с:
1. Результатами каждого теста
2. Найденными уязвимостями
3. Рекомендациями по исправлению
4. Статусом безопасности для каждого тарифа

---

## 🔄 Регулярное тестирование

- **Еженедельно:** Базовые тесты безопасности
- **После каждого обновления:** Полное тестирование
- **Перед релизом:** Комплексное тестирование всех тарифов

